extends Node
var upperboundary : int = 1800 ###pixel dependent, will change when target resolution is decided
var lowerboundary : int = 1400 ###pixel dependent, will change when target resolution is decided
var bpm_debug = float(90.0 / 60) #####for testing bpm speed scaling, technically units of beats per second, applied as a speed multiplier to animations looping on one second

## Called when the node enters the scene tree for the first time.
#func _ready():
	#pass



#func _process(delta):
	#pass
